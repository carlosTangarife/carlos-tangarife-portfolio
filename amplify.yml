version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
      - '_next/**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
  appRoot: .